# Callout
Full-width strip: surface bg, 1px border, 2px signal-colored LEFT border only.
Leading mono caps label (NOTE / WARN / FAIL) in the signal color, body in
15px Inter. Kinds: note (blue), warn (orange), fail (red). Don't stack more
than one callout per section.
