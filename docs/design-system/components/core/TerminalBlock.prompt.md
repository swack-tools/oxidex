# TerminalBlock
CLI sample: prompt green, command orange, output dim — the same pairing rule
as everything else. Body sits one shade darker than the page (#0a0c0e), the
only place that shade exists. COPY copies command lines only.
