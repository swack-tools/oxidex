# Card
Surfaces are stepped, not shadowed: surface bg + 1px hairline border, 4px
radius, 24px padding. `title` renders a mono caps label that interrupts the
top border like a fieldset legend (label bg = page bg, so titled cards sit
directly on --ox-bg, not on another surface). `hero` adds the orange glow —
exactly one hero per page.
