# StatTile
One big mono number (56px, normal weight), a 13px dim caps label under it,
optional ▲/▼ delta in green/red. No icon, no sparkline, no card wrapper —
compose inside a Card if a border is needed.
