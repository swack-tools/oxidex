# Badge
Two families. PASS/FAIL/WIP: 2px-radius rect, 12%-alpha fill, 1px signal
border, mono caps. Format support: leading 8px dot — green (supported),
half-opacity green (partial), border-gray (unsupported) — with the label in
full-brightness text. Never mix the two families in one row group.
