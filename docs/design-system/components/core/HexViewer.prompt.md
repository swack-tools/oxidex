# HexViewer
Offset gutter (dim), 16 byte pairs with a mid-row gap, ASCII column (dim,
unprintables as ·). Decorative or real. The orange span is "the bytes that
matter" — never highlight more than one span, and caption it with the
└─ label when meaning isn't obvious.
