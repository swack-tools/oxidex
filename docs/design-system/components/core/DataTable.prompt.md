# DataTable
The workhorse. Dense mono rows: keys dim, values green, offsets in the dim
gutter column. surface-2 header with 12px caps labels, 1px row hairlines,
surface-2 hover. No zebra striping, no sort icons — keep it terminal-flat.
