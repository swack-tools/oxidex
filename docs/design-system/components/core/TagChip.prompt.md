# TagChip
The signature component: a small mono chip like `0x0110 · Model`. Dim hex
prefix + colored label, 2px radius, never wraps. Tones: neutral (plain),
group (orange — EXIF/XMP/IPTC group names), value (green — tag has a value),
error (red). Border tints to a 35%-alpha version of the tone color.
