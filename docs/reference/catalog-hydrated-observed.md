# Catalog observations

The authenticated historical snapshot is awaiting a fresh common-runtime publication. The source ledger does not award observed read or write credit until native comparison receipts are imported.

Verified checkpoints and their separate denominators are recorded in [the parity checkpoint](goal-checkpoint-20260914.md). They must not be combined into a current corpus-wide coverage percentage.

The publisher validates the native receipts while their build, fixture and oracle inputs are available. Later CI checks the stored historical bindings; it does not rerun native tools or claim that a changed runtime has the same behavior.
