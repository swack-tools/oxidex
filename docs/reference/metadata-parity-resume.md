# Resume metadata parity work

Resumed by the maintainer on 2026-09-15 after the tranche consolidated in PR [#779](https://github.com/swack-tools/oxidex/pull/779). The first resumed milestone is the generated Garmin FIT reader ([source review](garmin-fit-source-review.md)). PRs #682 and #683 remain separate.

## Read first

- [Checkpoint and exact denominators](goal-checkpoint-20260914.md)
- [Source classifications by table](catalog-hydrated-join.md)
- [Authenticated historical observations](catalog-hydrated-observed.md)
- [Review and validation record](parity-rollup-review-20260914.md)
- [Migration plan](source-family-migration-plan.md)

All 33,487 catalog entries are classified. This is inventory completeness, not read/write parity. The historical common-runtime snapshot has observed evidence for 26 catalog entries read and 19 written. The full catalog-wide parity objective remains incomplete.

## First actions when resuming

1. Fetch `origin`, inspect the merged PR and current `origin/refactor/tag-machinery`, and check the root `HANDOFF.md` for the last validation receipts and owned process state.
2. Create one new isolated worktree and `codex/` branch from that verified base. Preserve existing evidence and other agents' worktrees. Do not edit the protected checkout.
3. Use `.exiftool-version`, an explicit Perl executable and the pinned library. Record interpreter, source commit and binary identity with every measurement.
4. Select the largest useful refusal block in the generated per-table ledger. Implement one shared protocol or conversion capability, then regenerate its rows and refusals together. Do not add tag-name exceptions to increase a percentage.
5. Validate each distinct binary behavior with actual native comparisons. Credit observed reads and writes only to exercised catalog coordinates; keep declaration counts separate.

Heavy i7 work must hold `/tmp/i7-heavy.lock`; heavy M4 work must hold `/tmp/m4-heavy.lock`. Process-name checks are insufficient. Preserve the binaries and native receipts referenced by historical snapshots. The local handoff records the shared local gate lock and exact evidence locators.

## What remains after this landing

- Extend generated readers and writers beyond the current bounded QuickTime and public scalar subsets.
- Convert specific source refusals into shared capabilities, accounting for newly accepted and still blocked rows on every regeneration.
- Expand authenticated observations with native reads and real write/readback, reporting catalog entries, Group1 names, occurrences and operations separately.
- Resume the preserved 11.78 historical caller refusal by modeling its exact shared `SetNewValue` control flow; the native probe passed but generation refused, and no read/write or 12.64 result exists.
- Continue mechanical upgrade rehearsals: regenerate against the selected source, inspect row and behavior changes, and refuse unmodeled semantics rather than guessing.

No claim of 100% autogenerated tags or full read/write parity is warranted by this checkpoint.
